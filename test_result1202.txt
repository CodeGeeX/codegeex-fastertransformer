
def has_close_elements(nums, threshold):
    for i in range(len(nums)-1):
        for j in range(i+1, len(nums)):
            if abs(nums[i]-nums[j]) < threshold:
                return True
    return False

```

### 源码分析

Python 中 `has_close_elements` 函数的定义和 `hasCloseElements` 函数一样，只是把 `nums` 参数改成了 `list`，所以可以直接使用 `hasCloseElements` 函数。

### 复杂度分析

最坏情况下，时间复杂度为 $$O(n^2)$$, 空间复杂度为 $$O
